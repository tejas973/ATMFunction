import { Meteor } from 'meteor/meteor';
import '../imports/api/cardDetails.js';
import '../imports/api/atmDetails.js';
import '../imports/api/transaction.js';
Meteor.startup(() => {
  // code to run on server at startup
});
